[package]
name = "mimalloc-pprof"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "mimalloc global allocator with pprof support"
links = "mimalloc"
build = "build.rs"

[dependencies]

[build-dependencies]
cc = "1.2"

[dev-dependencies]
regex = "1"
